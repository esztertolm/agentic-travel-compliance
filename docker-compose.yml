version: '3.8'

services:
  app:
    build: .
    container_name: agentic_travel_compliance_app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - /app/chroma_db
      - /app/logs
    environment:
      - PYTHONUNBUFFERED=1